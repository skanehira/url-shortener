apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: shortener-service
spec:
  ingressClassName: nginx
  rules:
    - host: url-shortener.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: prod-shortener-service
                port:
                  name: http
  tls:
    - hosts:
        - url-shortener.example.com
      secretName: shortener-tls
