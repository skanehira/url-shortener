apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: shortener-service
spec:
  parentRefs:
    - name: shortener-gateway
      namespace: url-shortener-prod
  hostnames:
    - "url-shortener.prod.k8s.local"
