# This file contains placeholder SealedSecrets.
# Replace these with actual SealedSecrets generated using kubeseal.
# See README.md for instructions.

# Placeholder for postgres-credentials
# Generate with:
# kubectl create secret generic postgres-credentials \
#   --namespace url-shortener \
#   --from-literal=username=urlshortener \
#   --from-literal=password='YOUR_PASSWORD' \
#   --dry-run=client -o yaml | kubeseal --format yaml
---
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: postgres-credentials
  labels:
    app.kubernetes.io/name: postgres-credentials
    app.kubernetes.io/component: secrets
    app.kubernetes.io/part-of: url-shortener
spec:
  encryptedData:
    username: "REPLACE_WITH_SEALED_VALUE"
    password: "REPLACE_WITH_SEALED_VALUE"
  template:
    metadata:
      name: postgres-credentials
    type: Opaque

---
# Placeholder for rabbitmq-credentials
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: rabbitmq-credentials
  labels:
    app.kubernetes.io/name: rabbitmq-credentials
    app.kubernetes.io/component: secrets
    app.kubernetes.io/part-of: url-shortener
spec:
  encryptedData:
    username: "REPLACE_WITH_SEALED_VALUE"
    password: "REPLACE_WITH_SEALED_VALUE"
  template:
    metadata:
      name: rabbitmq-credentials
    type: Opaque

---
# Placeholder for url-shortener-secrets (application secrets)
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: url-shortener-secrets
  labels:
    app.kubernetes.io/name: url-shortener-secrets
    app.kubernetes.io/component: secrets
    app.kubernetes.io/part-of: url-shortener
spec:
  encryptedData:
    database-url: "REPLACE_WITH_SEALED_VALUE"
    redis-url: "REPLACE_WITH_SEALED_VALUE"
    rabbitmq-url: "REPLACE_WITH_SEALED_VALUE"
  template:
    metadata:
      name: url-shortener-secrets
    type: Opaque
